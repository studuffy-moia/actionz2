name: Main
on:
  push:
    branches:
      - main

jobs:
  run-action-from-other-repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: studuffy-moia/actionz/.github/actions/test
        with:
          who-to-greet: "stu"
